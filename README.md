# Cloud Static Site — GitHub Pages + Actions

Simple static website deployed via GitHub Pages with continuous deployment using GitHub Actions.

## How it works
- Push changes to the `main` branch.
- GitHub Actions workflow deploys the `index.html` and `style.css` to GitHub Pages.

## Live URL
Will be: `https://<your-username>.github.io/cloud-static-site`

## Local
Open `index.html` in a browser to preview locally.
